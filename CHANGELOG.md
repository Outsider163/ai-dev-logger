# 版本变更记录

## v1.2.2

### 使用体验

- 所有公开命令提供中文帮助和常用示例，命令名与参数名保持不变。
- 交互记笔记模式和 DeepSeek 配置向导提供中文提示。
- 命令失败时保留具体错误原因，并提示对应的 `adl ... --help` 用法入口。
- 配置命令拒绝多余的位置参数，避免输入错误时仍然修改配置。

### 自检诊断

- `doctor` 分别报告本地笔记、AI 整理、语义检索三项能力的状态。
- 未配置 API Key、聊天模型或向量模型时提示 `WARN`，不再将缺少可选 AI 能力视为整体失败。
- `doctor --online` 独立检查配置完整的聊天和向量接口；只配置聊天功能时可以跳过向量检查。
- 明确区分“已配置但未联网验证”和“本次接口验证通过”，能力汇总不会重复计入失败次数。
- 配置文件损坏、数据库错误、无效 API 地址或已配置接口验证失败时，仍返回非零退出码。

### 测试与发布保障

- CI 同时在 Ubuntu 和 Windows 上运行；正式发布先通过 Windows 命令与安装器验收。
- 补齐默认配置、文件读写、损坏文件、密钥优先级、脱敏和局部配置更新的测试。
- 验证修改模型不会将环境变量中的密钥写回配置文件，非法参数和损坏配置不会触发覆盖保存。
- Windows 安装验收新增帮助页无副作用、配置读写与脱敏、无 AI 配置的离线自检检查。

### 升级说明

- 本版不修改 SQLite 数据库结构、默认数据目录或笔记 JSON 导出格式，不需要重新导入已有笔记。
- 现有命令名、参数名和结构化数据字段不变；帮助文字、错误前缀和 `doctor` 报告有调整。
- 依赖终端输出的脚本需要留意新的错误前缀和自检能力汇总；缺少可选 AI 配置不再导致 `doctor` 失败。
- 密钥读取顺序保持为 `AI_DEV_LOGGER_API_KEY`、配置文件、`OPENAI_API_KEY`。
- 没有增加新的 AI 服务接口；聊天和向量功能仍使用同一组 API 地址和有效密钥，模型名称分别配置。
- 学习笔记、本地数据库、真实配置和构建产物不纳入本次源码发布。
