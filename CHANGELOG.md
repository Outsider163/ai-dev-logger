# 版本变更记录

## Unreleased

- 新增 `eval compare before.json after.json`：离线对比版本 2 评估报告，显示指标差值、检索变化及退步题目；校验题目一致性并重新核算报告分数，不调用模型或打开数据库。

- `eval` 支持用空预期编号数组测试无答案问题，单独统计正确拒绝率并新增 `--min-abstention-rate` 门槛。JSON 报告升级至版本 2，缺少题型的指标为 null，提供七题混合评估示例；仅验证检索层面，不验证生成模型拒答。

- `eval` 新增 JSON 报告和最低命中率、召回率、MRR 门槛；未达标时先输出完整报告再返回失败，便于脚本进行质量回归检查。

- 新增 `eval --input cases.json`：基于预期笔记编号评估检索命中率、平均召回率和 MRR，复用问答的片段选择与预算，仅调用向量服务。提供独立数据库的五题演示资料和验收指南。

- 新增 `ask --retrieve-only` 检索诊断模式：展示实际选中资料、来源分数和字符用量，复用正常问答选择逻辑，仅调用向量服务，不要求聊天配置。

- `ask` 支持同一笔记多个相关片段，新增 `--chunks-per-note`（默认 2）和 `--context-chars`（默认 12000）；优先覆盖不同笔记，按实际资料文本控制字符预算并展示来源用量。`--limit` 限制为 1 到 20。

- 新增 `ingest --sync` 来源同步：按绝对路径关联稳定笔记 ID，正文变化时事务化更新切片、清理摘要和旧向量；正文双向修改冲突时整批回滚。支持预演，`show` 显示来源路径。
- 数据库升级为 schema 3，新增级联删除的来源表；完整数据库备份保留来源关系，JSON 导出仍保持原格式。升级前建议备份，旧程序不能打开升级后的库。

- 新增 `ingest <file> [file...]`：将 UTF-8 文本、Markdown 和代码文件事务化导入笔记，保留原文并自动切片，支持标签、预演及重复策略。
- `ingest` 支持目录、`--recursive` 子目录扫描及 `--ext` 类型筛选，预演显示文件清单，跳过隐藏项、常见依赖/构建目录和符号链接，按相对路径生成标题。

- 新增自动切片：每片最多 1200 个 Unicode 字符，优先按段落或换行分割；新增、修改、导入事务化维护切片，删除级联清理。支持 `show <id> --chunks` 查看。
- 向量索引改为按片段保存；整条笔记的全部片段有效才跳过生成，API 中途失败或源笔记变化时不会写入半套新索引。语义检索按最佳片段对笔记去重，并向 AI 提供命中片段正文。
- 数据库升级至 schema 2，自动补齐旧笔记切片并保留旧向量；长笔记旧向量需 `embed --all` 重建。升级前建议备份，旧程序不能打开升级后的数据库。
- 交互模式支持直接输入 `list`、`search`、`show`、`help`、`exit`，兼容原来的斜杠命令和 `adl` 前缀，避免命令被误存为笔记。
- 支持交互修改标题、正文和标签，复用已有更新规则并清理旧向量；支持显示编号和标题后确认删除，默认取消。
- 增加 `add 内容` 明确录入命令词开头的笔记，校验命令参数；不执行系统命令、不展开环境变量、不调用 AI。
- 笔记 ID 规则不变：删除后不重新编号、不复用旧编号。PowerShell 中原有命令和参数保持不变。
- 补充交互模式回归测试、Windows 实际程序验收与增删改查指南。
- 聊天与向量服务支持独立 API 地址、模型和密钥，可组合使用不同的 OpenAI 兼容供应商；旧版共享配置和环境变量继续兼容。
- 新增 `ask` 知识问答：先检索本地笔记片段，再让聊天模型依据命中内容回答并引用 `[Note #编号]`；无足够相关资料时不调用聊天服务。

## v1.2.2

### 使用体验

- 所有公开命令提供中文帮助和常用示例，命令名与参数名保持不变。
- 交互记笔记模式和 DeepSeek 配置向导提供中文提示。
- 命令失败时保留具体错误原因，并提示对应的 `adl ... --help` 用法入口。
- 配置命令拒绝多余的位置参数，避免输入错误时仍然修改配置。

### 自检诊断

- `doctor` 分别报告本地笔记、AI 整理、语义检索三项能力的状态。
- 未配置 API Key、聊天模型或向量模型时提示 `WARN`，不再将缺少可选 AI 能力视为整体失败。
- `doctor --online` 独立检查配置完整的聊天和向量接口；只配置聊天功能时可以跳过向量检查。
- 明确区分“已配置但未联网验证”和“本次接口验证通过”，能力汇总不会重复计入失败次数。
- 配置文件损坏、数据库错误、无效 API 地址或已配置接口验证失败时，仍返回非零退出码。

### 测试与发布保障

- CI 同时在 Ubuntu 和 Windows 上运行；正式发布先通过 Windows 命令与安装器验收。
- 补齐默认配置、文件读写、损坏文件、密钥优先级、脱敏和局部配置更新的测试。
- 验证修改模型不会将环境变量中的密钥写回配置文件，非法参数和损坏配置不会触发覆盖保存。
- Windows 安装验收新增帮助页无副作用、配置读写与脱敏、无 AI 配置的离线自检检查。

### 升级说明

- 本版不修改 SQLite 数据库结构、默认数据目录或笔记 JSON 导出格式，不需要重新导入已有笔记。
- 现有命令名、参数名和结构化数据字段不变；帮助文字、错误前缀和 `doctor` 报告有调整。
- 依赖终端输出的脚本需要留意新的错误前缀和自检能力汇总；缺少可选 AI 配置不再导致 `doctor` 失败。
- 密钥读取顺序保持为 `AI_DEV_LOGGER_API_KEY`、配置文件、`OPENAI_API_KEY`。
- 没有增加新的 AI 服务接口；聊天和向量功能仍使用同一组 API 地址和有效密钥，模型名称分别配置。
- 学习笔记、本地数据库、真实配置和构建产物不纳入本次源码发布。
